package dataStructures.pseudoQueue;

import dataStructures.queue.Queue;

public interface PseudoQueueInterFace {


     void enqueue(PseudoQueue queue , int value);
     int dequeue(PseudoQueue queue);
}
